{
  "buildCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then npm run db:push && next build; else next build; fi",
  "crons": [
    {
      "path": "/api/cron/cleanup",
      "schedule": "0 6 * * *"
    }
  ]
}
